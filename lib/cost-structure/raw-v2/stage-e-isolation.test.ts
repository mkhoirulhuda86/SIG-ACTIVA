import assert from 'node:assert/strict';import test from 'node:test';import{readFileSync}from'node:fs';
test('Stage E stays isolated from legacy transaction tables and production migration commands',()=>{const service=readFileSync('lib/cost-structure/raw-v2/si-service.ts','utf8');const migration=readFileSync('prisma/migrations/20260904210000_add_raw_v2_stage_e_si/migration.sql','utf8');assert.doesNotMatch(service,/\.cost(?:Period|Upload|SourceRow|ValidationIssue|CalculationRun|ActualLine|CalculationResult)\b|activeCalculationRunId/);assert.doesNotMatch(migration,/^\s*(?:DROP|DELETE|TRUNCATE)\b/im);assert.match(service,/TransactionIsolationLevel\.Serializable/);});
test('Stage E API enforces PREPARE on calculation and READ on results',()=>{assert.match(readFileSync('app/api/cost-structure/raw-v2/si/calculate/route.ts','utf8'),/requireCostStructurePrepare/);assert.match(readFileSync('app/api/cost-structure/raw-v2/si/route.ts','utf8'),/requireCostStructureRead/);});
test('Stage E runtime has no hard-coded acceptance COA or August amounts',()=>{const runtime=readFileSync('lib/cost-structure/raw-v2/si.ts','utf8')+readFileSync('lib/cost-structure/raw-v2/si-service.ts','utf8');assert.doesNotMatch(runtime,/62140001|147739445941|3448504987|159829527515/);});
